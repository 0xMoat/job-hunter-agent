<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <title>{{ resume.name_zh }} — 简历</title>
  <style>
    @page { size: A4 portrait; margin: 15mm 20mm; }
    body { font-family: "Noto Sans CJK SC", "Noto Sans SC", "PingFang SC", "Microsoft YaHei", sans-serif; font-size: 10pt; line-height: 1.6; color: #222; margin: 0; padding: 0; }
    h1 { font-size: 18pt; margin: 0 0 2mm; }
    h2 { font-size: 11pt; border-bottom: 1px solid #ccc; padding-bottom: 1mm; margin: 5mm 0 3mm; }
    h3 { font-size: 10pt; margin: 2mm 0 1mm; }
    h4 { font-size: 9pt; margin: 1mm 0; }
    .header { text-align: center; margin-bottom: 5mm; }
    .name-en { font-size: 9pt; color: #666; }
    .contact { font-size: 8pt; color: #555; margin-top: 2mm; }
    .contact span { margin: 0 4px; }
    .summary { margin-bottom: 4mm; font-size: 9pt; }
    .skill-group { margin-bottom: 2mm; font-size: 9pt; }
    .job-period { font-size: 8pt; color: #666; float: right; }
    .job-role { font-size: 9pt; color: #555; margin-bottom: 2mm; }
    .sub-project { margin-left: 4mm; margin-bottom: 2mm; }
    ul { margin: 0; padding-left: 5mm; }
    li { font-size: 9pt; margin-bottom: 1px; }
    .project { margin-bottom: 3mm; }
    .project-header { font-weight: bold; font-size: 10pt; }
    .project-status { font-size: 8pt; color: #666; font-style: italic; }
    .edu { margin-bottom: 2mm; }
    .edu-school { font-weight: bold; }
    .edu-meta { font-size: 8pt; color: #666; }
    strong { color: #222; }
    code { font-family: monospace; font-size: 8pt; background: #f5f5f5; padding: 0 2px; }
  </style>
</head>
<body>

  <div class="header">
    <div class="name-en">{{ resume.name_en }}</div>
    <h1>{{ resume.name_zh }}</h1>
    <div style="font-size:10pt;color:#555;">{{ resume.current_focus }}</div>
    <div class="contact">
      <span>{{ resume.contact.location }}</span> |
      <span>{{ resume.contact.phone }}</span> |
      <span>{{ resume.contact.email }}</span>
      {% if resume.contact.github %} | <span>GitHub: {{ resume.contact.github }}</span>{% endif %}
      {% if resume.contact.linkedin %} | <span>LinkedIn: {{ resume.contact.linkedin }}</span>{% endif %}
    </div>
  </div>

  <h2>个人总结</h2>
  <div class="summary">{{ resume.summary }}</div>

  <h2>核心技能</h2>
  {% for group in resume.skills %}
  <div class="skill-group">
    <strong>{{ group.domain }}：</strong>
    {% for item in group.items %}{{ item.name }}{% if not loop.last %}、{% endif %}{% endfor %}
  </div>
  {% endfor %}

  {% if resume.projects %}
  <h2>个人项目</h2>
  {% for proj in resume.projects %}
  <div class="project">
    <span class="project-header">{{ proj.name }}</span>
    <span class="project-status"> — {{ proj.status }}</span>
    {% if proj.url %}<span style="font-size:8pt;"> ({{ proj.url }})</span>{% endif %}
    <div style="font-size:9pt;color:#555;margin:1mm 0;">{{ proj.description }}</div>
    <ul>
      {% for point in proj.points %}
      <li>{{ point | bold_code }}</li>
      {% endfor %}
    </ul>
  </div>
  {% endfor %}
  {% endif %}

  <h2>工作经历</h2>
  {% for job in resume.experience %}
  <div style="margin-bottom:4mm;">
    <div>
      <span class="job-period">{{ job.dates }}</span>
      <h3 style="margin:0;">{{ job.company }}</h3>
    </div>
    <div class="job-role">{{ job.role }}</div>
    {% for sp in job.sub_projects %}
    <div class="sub-project">
      <h4>{{ sp.name }}</h4>
      <ul>
        {% for point in sp.points %}
        <li>{{ point | bold_code }}</li>
        {% endfor %}
      </ul>
    </div>
    {% endfor %}
  </div>
  {% endfor %}

  <h2>教育背景</h2>
  {% for edu in resume.education %}
  <div class="edu">
    <span class="edu-school">{{ edu.school }}</span>
    <span class="edu-meta"> — {{ edu.degree }} | {{ edu.dates }}</span>
  </div>
  {% endfor %}

</body>
</html>
